#include <stdio.h>
#include <stdlib.h>
#include "lists.h"

/**
 *
 *
 *
 *
 *
 *
*/

size_t print_dlistint(const dlistint_t *h)
{
	size_t count = 0;

	dlistint_t *new_node;
	
	new_node = malloc(sizeof(dlistint_t));

	new_node->n;
	new_node->prev;
	new_node->next;

	if (head == NULL)
	{
		head = new_node;
	}
}
