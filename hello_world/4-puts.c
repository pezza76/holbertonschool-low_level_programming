
#include <stdio.h>

/**
 * main function - Prints  "Programming is like building a multilingual puzzle, followed by a new line.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
